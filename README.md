# Simpsons-character-image-classifier
A custom convolutional neural network trained from scratch to classify Simpsons characters from images. Achieves high Macro F1 across all character classes using weighted sampling, label smoothing, and cosine annealing — without any pretrained backbone.
